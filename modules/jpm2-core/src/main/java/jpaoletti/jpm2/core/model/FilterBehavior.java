package jpaoletti.jpm2.core.model;

/**
 * Filter behavior, is quite simple by now but its something to avoid the "AND"
 * behavior.
 * 
 * @author jpaoletti
 */
public enum FilterBehavior {
    AND,
    OR
}
